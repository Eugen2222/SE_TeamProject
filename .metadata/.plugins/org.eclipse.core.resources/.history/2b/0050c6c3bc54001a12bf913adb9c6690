package DefaultPackage;
import ModelPackage.Model;
import ControllerPackage.*;
import GUIPackage.View;

public class Run {
	public static void main (String[] args) {
		
		View v = new View();
		Model model = new Model();
		String [] testUser = {"123", "123123123"};
		int usertype = model.verify(testUser);
		if(usertype == 1) {
			CDController controller = new CDController(model);
		}
}
